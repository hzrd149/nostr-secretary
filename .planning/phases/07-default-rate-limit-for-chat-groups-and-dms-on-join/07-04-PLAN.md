---
phase: 07-default-rate-limit-for-chat-groups-and-dms-on-join
plan: 04
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - pages/groups.tsx
  - pages/messages.tsx
autonomous: true
requirements: [D7-08, D7-06]
must_haves:
  truths:
    - "The /groups page shows a SECOND, distinctly-named number input (rateLimitPerGroup) alongside the existing rateLimitPerType field, bound to rateLimit.perGroup (D7-08)."
    - "The /messages page shows a SECOND, distinctly-named number input (rateLimitPerDm) alongside the existing rateLimitPerType field, bound to rateLimit.perDm (D7-08)."
    - "Each new field's PATCH handler clamps the submitted signal to a non-negative integer (finite >= 0, floored), falling back to the current value otherwise (ASVS V5)."
    - "perGroup/perDm are merged into newConfig.rateLimit as TOP-LEVEL siblings of perType, preserving the existing perType field on save (D7-08)."
    - "Each new field's help text states 0 = unlimited (Pitfall 5)."
  artifacts:
    - pages/groups.tsx
    - pages/messages.tsx
  key_links:
    - "The new inputs use a DISTINCT signal name (rateLimitPerGroup/rateLimitPerDm) — reusing rateLimitPerType would collide with the existing per-type field's PATCH read."
    - "The PATCH merge must spread the existing rateLimit and set perGroup/perDm as top-level siblings, never nested in perType."
---

<objective>
Add the two default-limit UI fields (D7-08): a `rateLimitPerGroup` number input on `pages/groups.tsx`
and a `rateLimitPerDm` number input on `pages/messages.tsx`, each a SECOND field ADDED ALONGSIDE the
existing Phase-6 `rateLimitPerType` field (both pages already have one rate-limit input — Pitfall 7).
Each uses a distinctly-named Datastar signal, is bound to `rateLimit.perGroup`/`.perDm`, clamps the
submitted value to a non-negative integer in the PATCH handler (mirroring the existing
`rateLimitPerType` clamp verbatim), merges the value as a TOP-LEVEL sibling of `perType`, and shows
help text including "0 = unlimited". No per-specific-group/DM override UI (deferred).

Purpose: Let the user set the shared default per-group / per-DM limits from the existing settings
pages.
Output: Two extended pages with a second rate-limit field each.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-default-rate-limit-for-chat-groups-and-dms-on-join/07-CONTEXT.md
@.planning/phases/07-default-rate-limit-for-chat-groups-and-dms-on-join/07-RESEARCH.md
@.planning/phases/07-default-rate-limit-for-chat-groups-and-dms-on-join/07-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this plan produces (NEW symbols)

- `rateLimitPerGroup` Datastar signal + number input on pages/groups.tsx, bound to currentConfig.rateLimit.perGroup.
- `rateLimitPerDm` Datastar signal + number input on pages/messages.tsx, bound to currentConfig.rateLimit.perDm.
- A PATCH clamp block per page (rawRateLimitPerGroup/rawRateLimitPerDm → non-negative int, current-value fallback).
- A top-level `perGroup`/`perDm` merge into each page's newConfig.rateLimit (sibling of perType).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add the default-per-group field to /groups</name>
  <files>pages/groups.tsx</files>
  <read_first>
    - pages/groups.tsx (its OWN current form is the analog — existing rateLimitPerType input at lines 238-255; PATCH signal read line 301; clamp block lines 344-347; rateLimit merge lines 359-366).
    - services/config.ts (Plan 02's AppConfig.rateLimit.perGroup so `currentConfig.rateLimit.perGroup` type-checks).
    - 07-RESEARCH.md "Pattern 6: UI — a second, distinctly-named number input per page" and Pitfall 7 (this is a SECOND field, not the first) and Pitfall 5 (0 = unlimited help text).
  </read_first>
  <action>
    In the view, add a SECOND `form-group` block below the existing `rateLimitPerType` block: a
    number input with `id="rateLimitPerGroup"`, `data-bind="rateLimitPerGroup"`, `min="0"`,
    `value={String(currentConfig.rateLimit.perGroup)}`, a bold label like "Default Per-Group Rate
    Limit", and help text stating it caps notifications from any single group per window, is applied
    automatically to newly-joined groups, and "0 = unlimited" (Pitfall 5). Do NOT reuse the
    `rateLimitPerType` signal name (Pitfall 7 / Anti-Patterns).

    In the PATCH handler, read `const rawRateLimitPerGroup = Number(signals.rateLimitPerGroup)` and
    add a clamp block mirroring the existing `rateLimitPerType` one exactly:
    `Number.isFinite(rawRateLimitPerGroup) && rawRateLimitPerGroup >= 0 ? Math.floor(rawRateLimitPerGroup) : currentConfig.rateLimit.perGroup`.
    Merge it into `newConfig.rateLimit` as a TOP-LEVEL sibling of `perType`
    (`{ ...currentConfig.rateLimit, perGroup: rateLimitPerGroup, perType: { ...currentConfig.rateLimit.perType, groups: rateLimitPerType } }`)
    — NOT nested inside perType. Leave the existing per-type field, whitelist/blacklist, modes, and
    enabled handling untouched.
  </action>
  <verify>
    <automated>bun run lint && grep -q 'data-bind="rateLimitPerGroup"' pages/groups.tsx && grep -q 'perGroup:' pages/groups.tsx && grep -iq '0 = unlimited' pages/groups.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'rateLimitPerType' pages/groups.tsx` is unchanged from before (the existing per-type field/handler is preserved, not renamed).
    - Source assertion: `grep -q 'data-bind="rateLimitPerGroup"' pages/groups.tsx` and `grep -q 'rawRateLimitPerGroup' pages/groups.tsx` both match (new input + clamp present).
    - Source assertion: `grep -q 'perGroup: rateLimitPerGroup' pages/groups.tsx` matches (top-level merge).
    - Test command: `bun run lint` (tsc --noEmit) green.
    - Help text includes "0 = unlimited".
  </acceptance_criteria>
  <done>The /groups page has a second rateLimitPerGroup input bound to rateLimit.perGroup, clamped and merged as a top-level sibling of perType; lint green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the default-per-DM field to /messages</name>
  <files>pages/messages.tsx</files>
  <read_first>
    - pages/messages.tsx (its OWN current form is the analog — existing rateLimitPerType input at lines 111-128; PATCH signal read line 175; clamp block lines 199-202; rateLimit merge lines 213-219).
    - services/config.ts (Plan 02's AppConfig.rateLimit.perDm so `currentConfig.rateLimit.perDm` type-checks).
    - 07-RESEARCH.md "Pattern 6" and Pitfall 7 (second field) and Pitfall 5 (0 = unlimited).
  </read_first>
  <action>
    In the view, add a SECOND `form-group` block below the existing `rateLimitPerType` block: a
    number input with `id="rateLimitPerDm"`, `data-bind="rateLimitPerDm"`, `min="0"`,
    `value={String(currentConfig.rateLimit.perDm)}`, a bold label like "Default Per-DM Rate Limit",
    and help text stating it caps notifications from any single DM conversation per window, is applied
    automatically to new DM conversations, and "0 = unlimited". Do NOT reuse the `rateLimitPerType`
    signal name.

    In the PATCH handler, read `const rawRateLimitPerDm = Number(signals.rateLimitPerDm)` and add a
    clamp block mirroring the existing `rateLimitPerType` one:
    `Number.isFinite(rawRateLimitPerDm) && rawRateLimitPerDm >= 0 ? Math.floor(rawRateLimitPerDm) : currentConfig.rateLimit.perDm`.
    Merge it into `newConfig.rateLimit` as a TOP-LEVEL sibling of `perType`
    (`{ ...currentConfig.rateLimit, perDm: rateLimitPerDm, perType: { ...currentConfig.rateLimit.perType, messages: rateLimitPerType } }`).
    Leave the existing per-type field, the contacts/others enable toggles, sendContent, and
    whitelist/blacklist handling untouched.
  </action>
  <verify>
    <automated>bun run lint && grep -q 'data-bind="rateLimitPerDm"' pages/messages.tsx && grep -q 'perDm:' pages/messages.tsx && grep -iq '0 = unlimited' pages/messages.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'rateLimitPerType' pages/messages.tsx` is unchanged from before (existing per-type field preserved).
    - Source assertion: `grep -q 'data-bind="rateLimitPerDm"' pages/messages.tsx` and `grep -q 'rawRateLimitPerDm' pages/messages.tsx` both match.
    - Source assertion: `grep -q 'perDm: rateLimitPerDm' pages/messages.tsx` matches (top-level merge).
    - Test command: `bun run lint` (tsc --noEmit) green.
    - Help text includes "0 = unlimited".
  </acceptance_criteria>
  <done>The /messages page has a second rateLimitPerDm input bound to rateLimit.perDm, clamped and merged as a top-level sibling of perType; lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser PATCH form → page route | A client-submitted `rateLimitPerGroup`/`rateLimitPerDm` signal is untrusted (may be negative, NaN, non-numeric). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Tampering | PATCH handler accepting a negative/non-numeric rateLimitPerGroup/rateLimitPerDm signal | medium | mitigate | Inline clamp `Number.isFinite(x) && x >= 0 ? Math.floor(x) : currentValue` before merge, mirroring the existing rateLimitPerType clamp on both pages (ASVS V5). |
| T-07-10 | Tampering | New input reusing the existing rateLimitPerType signal name, silently breaking the per-type field's PATCH read | low | mitigate | Distinct signal names (rateLimitPerGroup/rateLimitPerDm); acceptance greps confirm the existing rateLimitPerType field is preserved. |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed this phase (07-RESEARCH.md Package Legitimacy Audit is intentionally empty). |
</threat_model>

<verification>
- `bun run lint` (tsc --noEmit) green — requires Plan 02's AppConfig.rateLimit.perGroup/.perDm.
- Source greps confirm the two new distinctly-named inputs, clamps, top-level merges, and preserved per-type fields.

## UAT (manual, live UI — deferred consistent with Phase 6)
- On /groups and /messages, confirm the new default field renders with the current value, saves via
  PATCH, persists across reload, shows "0 = unlimited" help text, and does not disturb the existing
  per-type field. (Requires the running UI + a signer for the encrypted sync round trip — UAT-only
  per 07-VALIDATION.md.)
</verification>

<success_criteria>
- Both pages have a second, distinctly-named default rate-limit field bound to perGroup/perDm.
- PATCH clamps to a non-negative int and merges as a top-level sibling of perType.
- Existing per-type fields preserved; lint green.
</success_criteria>

<output>
Create `.planning/phases/07-default-rate-limit-for-chat-groups-and-dms-on-join/07-04-SUMMARY.md` when done.
</output>
