---
phase: 07-default-rate-limit-for-chat-groups-and-dms-on-join
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - services/rate-limit.ts
  - notifications/groups.ts
  - notifications/messages.ts
  - tests/services/rate-limit.test.ts
autonomous: true
requirements: [D7-01, D7-03, D7-04, D7-09]
must_haves:
  truths:
    - "rateLimitedNotify accepts an optional `context` on its existing options bag and threads it into evaluate()'s 5th argument (D7-03)."
    - "The group notification site passes `encodeGroupPointer(group)` as its context; both DM sites pass the raw counterparty pubkey (D7-01/D7-03)."
    - "A NIP-04 and a NIP-17 message from the same counterparty share ONE `messages:<pubkey>` bucket — no transport-specific prefix (Pitfall 4)."
    - "The 4 non-chat call sites (replies/zaps) are unchanged and pass no context (D7-01)."
    - "rateLimitedNotify never logs the raw context value — log parity with the existing type-only line (Information Disclosure)."
    - "The flush-timer pipeline (distinctUntilChanged on clampWindowSeconds(window)) is untouched — a perGroup/perDm-only config write does NOT restart the flush interval (D7-09)."
  artifacts:
    - services/rate-limit.ts
    - notifications/groups.ts
    - notifications/messages.ts
    - tests/services/rate-limit.test.ts
  key_links:
    - "rateLimitedNotify's evaluate() call must pass `context` as the 5th arg AND spread the full config.rateLimit (now carrying perGroup/perDm from Plan 02) so the context gate is actually consulted."
    - "The context key at the DM sites is the raw `sender`/`rumor.pubkey`, identical across both transports."
---

<objective>
Thread the per-context key end-to-end (D7-03): add `context?: string` to `rateLimitedNotify`'s
EXISTING options-bag 3rd parameter (`InjectedDeps`), pass it straight into `evaluate()`'s new 5th
argument, and add the context at exactly the 2 chat-listener files / 3 call sites — the group site
passes `encodeGroupPointer(group)`, both DM sites (NIP-04 and NIP-17) pass the raw counterparty
pubkey with NO transport prefix so one counterparty shares a single bucket (Pitfall 4). The 4
non-chat call sites (replies/zaps) require zero changes. The flush-timer block stays untouched so a
perGroup/perDm-only config write never restarts the flush interval (D7-09; Phase-6 CR-01 fix). Do
not log the raw context value (privacy parity).

Purpose: Activate the most-restrictive-wins per-context gate at runtime for groups and DMs, additive
to Phase 6's per-type/global limiter.
Output: Extended choke point + 3 wired call sites + threading/regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-default-rate-limit-for-chat-groups-and-dms-on-join/07-CONTEXT.md
@.planning/phases/07-default-rate-limit-for-chat-groups-and-dms-on-join/07-RESEARCH.md
@.planning/phases/07-default-rate-limit-for-chat-groups-and-dms-on-join/07-PATTERNS.md
@.planning/phases/01-nip-29-group-notification-modes/01-CONTEXT.md
</context>

<artifacts_produced>
## Artifacts this plan produces (NEW symbols)

- `InjectedDeps.context?: string` on services/rate-limit.ts (new optional key on the existing bag).
- `rateLimitedNotify` destructures `{ context, now, send }` and passes `context` as evaluate()'s 5th arg.
- `{ context: encodeGroupPointer(group) }` at the groups.ts notification call site.
- `{ context: sender }` at the NIP-04 messages.ts site and `{ context: sender }` (sender = rumor.pubkey) at the NIP-17 site.
- New test cases in tests/services/rate-limit.test.ts: context threading (isolation via rateLimitedNotify) + a flush-timer no-restart regression case for a perGroup/perDm-only write.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread context through rateLimitedNotify + threading/regression tests</name>
  <files>services/rate-limit.ts, tests/services/rate-limit.test.ts</files>
  <read_first>
    - services/rate-limit.ts (its OWN current form is the analog — InjectedDeps lines 78-81, rateLimitedNotify lines 101-122, flush-timer block lines 163-175).
    - services/rate-limit-accounting.ts (Plan 01's extended evaluate signature — the 5th context arg).
    - services/config.ts (Plan 02's DEFAULT_RATE_LIMIT_CONFIG now carrying perGroup/perDm — the setRateLimit test helper's type widens accordingly).
    - 07-RESEARCH.md "Pattern 3: rateLimitedNotify — fold context into the existing options bag" and Pitfall 1 (do NOT touch the flush-timer block) and Pitfall 3 (resetRateLimitState discipline).
    - tests/services/rate-limit.test.ts (fakeSend + setRateLimit helpers lines 28-59; resetRateLimitState-per-case discipline).
  </read_first>
  <behavior>
    - rateLimitedNotify(type, options, { context, now, send }) passes context into evaluate as the 5th arg.
    - With perGroup=1 and two different group contexts, the first context's second notification in a window is accumulated (not sent) while the OTHER context's first notification is still sent — observed through the injected `send` recorder (per-context isolation end-to-end).
    - Calling rateLimitedNotify without a context (replies/zaps shape) delivers exactly as in Phase 6 (regression parity).
    - A config write that changes ONLY perGroup/perDm (window unchanged) does NOT restart the flush interval — the distinctUntilChanged(clampWindowSeconds(window)) projection is unchanged, so runFlush timing is unaffected.
  </behavior>
  <action>
    Add `context?: string` as a new optional key on the EXISTING `InjectedDeps` type (alongside
    now/send) — not a new positional parameter, keeping every existing test call and the 4 non-chat
    call sites source-compatible. In `rateLimitedNotify`, destructure `{ context, now, send }` and
    pass `context` as the 5th argument to the existing `evaluate(state, type, effectiveNow, { ...rateLimit, window: clampWindowSeconds(rateLimit.window) }, context)`
    call. Leave the `log("Notification accumulated ...", { type })` line unchanged — do NOT add
    `context` to it (privacy parity with the existing type-only log line). Do NOT touch the
    flush-timer subscription block (`configValue("rateLimit").pipe(map(...clampWindowSeconds(cfg.window)...), distinctUntilChanged(), switchMap(...))`)
    — it deliberately projects only the window so perGroup/perDm writes are ignored (07-RESEARCH.md
    Pitfall 1; Phase-6 CR-01 fix). Do NOT modify runFlush.

    Extend tests/services/rate-limit.test.ts (which imports the impure module — call
    `resetRateLimitState(now)` at the start of each new case, Pitfall 3). Add: a per-context
    isolation case using `setRateLimit` with a small perGroup and the injected `send` recorder to
    prove one group's overflow does not block another group; a no-context regression case; and a
    flush-timer no-restart case asserting a perGroup/perDm-only `config$.next` does not change the
    effective window / re-trigger the interval (assert via the clampWindowSeconds projection value or
    runFlush determinism, without waiting on real timers). Restore DEFAULT_RATE_LIMIT_CONFIG in the
    existing afterAll.
  </action>
  <verify>
    <automated>bun test tests/services/rate-limit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n 'context' services/rate-limit.ts` matches on InjectedDeps and the evaluate call.
    - Source assertion: `grep -c 'distinctUntilChanged' services/rate-limit.ts` is still 1 (flush-timer pipeline untouched, D7-09).
    - Source assertion: `grep -n 'log("Notification accumulated' services/rate-limit.ts` shows the log call still passes only `{ type }` (no context logged).
    - Test command: `bun test tests/services/rate-limit.test.ts` green, including the per-context isolation and flush-timer-no-restart cases.
  </acceptance_criteria>
  <done>rateLimitedNotify threads context into evaluate; the flush timer is untouched; isolation + regression + no-restart tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Pass the context key at the group + 2 DM notification call sites</name>
  <files>notifications/groups.ts, notifications/messages.ts</files>
  <read_first>
    - notifications/groups.ts (current rateLimitedNotify("groups", {...}) call at lines 143-148; encodeGroupPointer imported line 8, group in scope line 119).
    - notifications/messages.ts (current NIP-04 call at lines 224-229 with `sender` in scope from line 172; NIP-17 call at lines 318-326 with `const sender = rumor.pubkey` at line 268).
    - services/rate-limit.ts (Task 1's extended rateLimitedNotify signature — the context option).
    - 07-RESEARCH.md "Pattern 1: Context key derivation" and Pitfall 4 (both DM transports share one raw-pubkey bucket).
  </read_first>
  <action>
    In notifications/groups.ts, add a 3rd argument `{ context: encodeGroupPointer(group) }` to the
    existing `rateLimitedNotify("groups", { ... })` call — encodeGroupPointer is already imported and
    `group` is already in scope (no new import/lookup). In notifications/messages.ts, add
    `{ context: sender }` as the 3rd argument to BOTH DM call sites: the NIP-04 site (where `sender`
    comes from getLegacyMessageReceiver) and the NIP-17 site (where `sender = rumor.pubkey`). Use the
    raw counterparty pubkey with NO transport-specific decoration on either site, so a NIP-04 and a
    NIP-17 message from the same counterparty deliberately share one bucket (Pitfall 4). Do NOT touch
    replies.ts or zaps.ts. Do NOT change any gate/decrypt/classification logic — only add the 3rd
    argument to the three existing calls.
  </action>
  <verify>
    <automated>bun test && grep -q 'context: encodeGroupPointer(group)' notifications/groups.ts && [ "$(grep -c 'context: sender' notifications/messages.ts)" -eq 2 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c 'context: sender' notifications/messages.ts` equals 2 (both DM sites wired with the raw counterparty key, no transport prefix).
    - Source assertion: `grep -q 'context: encodeGroupPointer(group)' notifications/groups.ts` matches (group site wired).
    - Source assertion: replies.ts and zaps.ts pass no context — `grep -L 'context:' notifications/replies.ts notifications/zaps.ts` lists both files (unchanged, D7-01).
    - Test command: `bun test` full suite green (no regression in the DM/group listeners).
  </acceptance_criteria>
  <done>The group site passes encodeGroupPointer(group); both DM sites pass the raw counterparty pubkey; replies/zaps untouched; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notification listeners → rateLimitedNotify | Group pointer / DM counterparty pubkey crosses into the choke point as the context key. |
| rateLimitedNotify → log sink | A raw context value (group identifier or DM counterparty pubkey) could leak into logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Information Disclosure | rateLimitedNotify's overflow-accumulation log line potentially logging the raw context (group id / DM counterparty pubkey) | low | mitigate | Leave the existing `log(..., { type })` line unchanged — never add `context` to it; parity with Phase 6's type-only log. |
| T-07-07 | Spoofing/Tampering | Context-key collision between a group key and a DM key merging two contexts' buckets | low | mitigate | Composite `` `${type}:${context}` `` key (evaluate, Plan 01); group keys always contain a literal apostrophe from encodeGroupPointer, DM keys are 64-char hex — structurally disjoint. |
| T-07-08 | Tampering | Regressing the Phase-6 flush-timer distinctUntilChanged guard, starving the grouped summary | medium | mitigate | Do not touch the flush-timer block; a test asserts a perGroup/perDm-only config write does not restart the interval (D7-09). |
| T-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | No packages installed this phase (07-RESEARCH.md Package Legitimacy Audit is intentionally empty). |
</threat_model>

<verification>
- `bun test` full suite green (accounting, rate-limit, config, preferences, listeners).
- `bun run lint` (tsc --noEmit) green — this plan's evaluate() call requires both Plan 01 (context arg) and Plan 02 (AppConfig perGroup/perDm) present.
- Source greps confirm the 3 call sites wired and the flush-timer block untouched.
</verification>

<success_criteria>
- context threads from the 3 chat sites through rateLimitedNotify into evaluate's per-context gate.
- Both DM transports share one raw-pubkey bucket; replies/zaps unchanged.
- Flush-timer pipeline unchanged; no per-context data logged.
</success_criteria>

<output>
Create `.planning/phases/07-default-rate-limit-for-chat-groups-and-dms-on-join/07-03-SUMMARY.md` when done.
</output>
