---
phase: 01-nip-29-group-notification-modes
plan: 05
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - pages/notifications.tsx
autonomous: true
requirements: [D-05]
user_setup: []

must_haves:
  truths:
    - "The /notifications Groups card shows a per-group mode summary — counts of groups on All / Only @mentions / Muted — derived from config.groups.modes via summarizeGroupModes (D-05)"
    - "The three per-mode counts are color-coded per UI-SPEC 'Per-group mode status colors' rather than plain text: the 'All' count keeps the existing accent (#667eea) treatment, the 'mentions' count uses the amber tone (text #856404), and the 'muted' count uses the red tone (text #721c24) (D-05)"
    - "The Groups card still links through to /groups for editing and still shows its existing enabled/disabled status (D-05, no regression)"
    - "When the user has zero joined groups (empty modes and empty groups list), the card shows a 'No groups joined yet' message instead of a 0 · 0 · 0 count (UI-SPEC)"
  artifacts:
    - "pages/notifications.tsx — Groups card extended with a per-mode summary line using summarizeGroupModes(config.groups.modes)"
  key_links:
    - "summarizeGroupModes is imported from helpers/groups.ts and fed config$.getValue().groups.modes (read-only; no new observable, no PATCH — D-05)"
---

<objective>
Update the single Groups card on the /notifications settings view to surface a
per-group mode summary — counts of how many groups are set to All / Only @mentions
/ Muted — and keep its link through to /groups for editing. This satisfies the
roadmap's "NIP-29 groups section in the notifications settings view" (D-05) without
duplicating the full per-group list on /notifications.

Purpose: Read-only aggregate display of config.groups.modes. No new client
interactivity, no new observable, no PATCH — just render counts from the existing
config using the pure summarizeGroupModes helper built in Plan 01.

Output: `pages/notifications.tsx` Groups card with a summary line and a zero-groups
empty-state message.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-nip-29-group-notification-modes/01-CONTEXT.md
@.planning/phases/01-nip-29-group-notification-modes/01-RESEARCH.md
@.planning/phases/01-nip-29-group-notification-modes/01-PATTERNS.md
@.planning/phases/01-nip-29-group-notification-modes/01-UI-SPEC.md

# Files this plan modifies / depends on
@pages/notifications.tsx
@helpers/groups.ts
@.planning/phases/01-nip-29-group-notification-modes/01-01-SUMMARY.md
@.planning/phases/01-nip-29-group-notification-modes/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the per-group mode summary to the Groups card (D-05)</name>
  <files>pages/notifications.tsx</files>
  <read_first>
    - pages/notifications.tsx (NotificationsList at lines 198-295; the existing Groups card at lines 274-292; the groupsEnabled fetch idiom at lines 208-210; the .notification-description style at lines 47-51)
    - helpers/groups.ts (summarizeGroupModes signature and return shape { all, mentions, muted })
    - services/config.ts (config$ default export, getValue().groups.modes)
    - .planning/phases/01-nip-29-group-notification-modes/01-PATTERNS.md ("pages/notifications.tsx" section: import config$, call summarizeGroupModes, insert a summary line)
    - .planning/phases/01-nip-29-group-notification-modes/01-UI-SPEC.md (Groups summary card copy: "{all} all messages · {mentions} mentions only · {muted} muted"; zero-groups shows "No groups joined yet"; mode badge colors)
  </read_first>
  <action>
    In pages/notifications.tsx, import the default config$ from ../services/config and
    summarizeGroupModes from ../helpers/groups. In NotificationsList (already async),
    compute `const groupModeSummary = summarizeGroupModes(config$.getValue().groups.modes
    ?? {})`. Inside the existing Groups card's notification-info block (lines 274-292),
    add a summary line rendered in the existing `.notification-description` style (no
    new CSS class) showing the counts, using the UI-SPEC copy format
    "{all} all messages · {mentions} mentions only · {muted} muted". When the total of
    all three counts is zero AND the user has no joined groups, render "No groups joined
    yet" instead of a 0 · 0 · 0 line (per UI-SPEC). Color-code the three counts per the
    01-UI-SPEC.md "Per-group mode status colors" table using server-rendered
    classes/inline style (no new client signal needed for static color): the "All" count
    keeps the existing accent treatment (#667eea), the "mentions" count uses the amber
    tone (text #856404), and the "muted" count uses the red tone (text #721c24) — so the
    summary reads as color-coded per-mode counts rather than plain text, consistent with
    the /groups row badges (Plan 04). The counts and the Configure link remain the core
    requirement.

    Keep the existing Groups card structure otherwise intact: the 👥 Groups title, the
    existing enabled/disabled status span (groupsEnabled), and the "Configure" link to
    /groups all stay. Do not add a PATCH handler or any Datastar signal — this card is
    read-only (RESEARCH: "no new client interactivity needed"). Do not modify the
    Direct Messages, Replies, or Zaps cards, or the NotificationOverview stats.
  </action>
  <verify>
    <automated>bun run lint && grep -q "summarizeGroupModes" pages/notifications.tsx</automated>
    <human-check>Run `bun run dev`, open /notifications: the Groups card shows the mode summary counts (e.g. "1 all messages · 3 mentions only · 0 muted") matching what is set on /groups, still shows its enabled/disabled status, and the Configure link still goes to /groups. With zero joined groups it reads "No groups joined yet". (Manual UAT — no Playwright config this phase.)</human-check>
  </verify>
  <acceptance_criteria>
    - The Groups card renders a summary line from summarizeGroupModes(config$.getValue().groups.modes ?? {})
    - Counts reflect config.groups.modes and match the /groups selections
    - The three counts are color-coded per UI-SPEC: "All" keeps the accent (#667eea) treatment, "mentions" uses the amber tone (text #856404), "muted" uses the red tone (text #721c24) — not plain uncolored text
    - Zero-groups shows "No groups joined yet" rather than a 0 · 0 · 0 line
    - The existing enabled/disabled status span and the /groups Configure link are unchanged
    - No PATCH handler or new signal added; the other three cards and the overview stats are unchanged
    - `bun run lint` passes
  </acceptance_criteria>
  <done>The /notifications Groups card shows an accurate per-group mode summary and links to /groups (D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Read-only render of numeric mode counts from local config; no untrusted input crosses a boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-M5 | Information Disclosure / XSS | Rendering the mode summary | low | mitigate | summarizeGroupModes returns only integers; the card renders counts, not user-controlled group names, so no unescaped untrusted string is emitted |

**No package installs this phase** — no `T-01-SC` checkpoint required.

**Pre-existing / out of scope:** no auth on the /notifications route (project-wide, `.planning/codebase/CONCERNS.md`); read-only page, not changed by this plan.
</threat_model>

<verification>
- `bun run lint` — strict typecheck passes
- Manual UAT on `bun run dev`: Groups card counts match /groups selections; zero-groups empty state shows
- `bun test` (full suite) still green
</verification>

<success_criteria>
- Groups card shows per-mode counts from config.groups.modes (D-05)
- The three counts are color-coded per UI-SPEC (All = accent #667eea, mentions = amber #856404, muted = red #721c24)
- Link to /groups and existing status preserved
- Read-only, no new signals or PATCH
</success_criteria>

<output>
Create `.planning/phases/01-nip-29-group-notification-modes/01-05-SUMMARY.md` when done
</output>
